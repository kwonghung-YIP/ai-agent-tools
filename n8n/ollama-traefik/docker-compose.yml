services:
  traefik:
    image: traefik:v3.6
    container_name: traefik
    command:
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.websecure.http.tls=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.file.filename=/etc/traefik/tls_config.yml"
      - "--api.dashboard=true"
      - "--api.insecure=false"
    ports:
      - 80:80
      - 443:443
      - 8080:8080
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    configs:
      - source: traefik_tls_yml
        target: /etc/traefik/tls_config.yml
    secrets:
      - traefik_ssl_key
      - traefik_ssl_cert
  
  #whoami:
  #  image: traefik/whoami
  #  container_name: whoami
  #  labels:
  #    - "traefik.enable=true"
  #    - "traefik.http.routers.whoami.rule=Host(`whoami.docker.localhost`)"
  #    - "traefik.http.routers.whomai.entrypoints=web"

  n8n:
    image: docker.n8n.io/n8nio/n8n:2.10.4
    container_name: n8n
    #ports:
    #  - 5678:5678
    volumes:
      - n8n_data:/home/node/.n8n
    environment:
      #- N8N_HOST="n8n.hung.org"
      #- N8N_HOST="localhost"
      #- N8N_PORT=80
      #- N8N_PROTOCOL=http
      #- N8N_LISTEN_ADDRESS="0.0.0.0"
      - GENERIC_TIMEZONE="Europe/London"
      - TZ="Europe/London"
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_RUNNERS_ENABLED=true
      #- N8N_SECURE_COOKIE=false
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.n8n.rule=Host(`n8n.hung.org`)"
      - "traefik.http.routers.n8n.entrypoints=websecure"
      - "traefik.http.routers.n8n.tls=true"

volumes:
  ollama_data:
    name: "ollama-data"
  n8n_data:
    name: "n8n-data"

secrets:
  traefik_ssl_key:
    file: certs/traefik.key
  traefik_ssl_cert:
    file: certs/traefik.crt

configs:
  traefik_tls_yml:
    content: |
      tls:
        certificates:
          - keyFile: /run/secrets/traefik_ssl_key
            certFile: /run/secrets/traefik_ssl_cert